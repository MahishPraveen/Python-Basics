a=int(input("Enter a"))
b=int(input("Enter b"))
c=int(input("Enter c"))
if a>b:
    if a>c:
        print(a,"is greatest number")
    else:
        print(c,"is greatest number")
else:
    if b>c:
        print(b,"is greatest number")
    else:
        print(c,"is greatest number")
